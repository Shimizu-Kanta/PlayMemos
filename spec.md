# 遊んだ人記録アプリ 仕様書（MVP）

## 概要
ゲーム・マダミス・TRPGなどで「いつ・誰と・何を遊んだか」を記録する個人用Webアプリ。
当面は開発者本人のみが使うが、将来は友人にも使ってもらう前提で、全データをユーザー単位で分離する。

## 技術スタック
- Next.js（App Router）+ TypeScript
- Supabase（PostgreSQL / Auth / RLS）
- 認証：Supabase Auth の Discord プロバイダ
- スタイル：Tailwind CSS
- カレンダー：FullCalendar（月表示）
- Supabaseクライアント：`@supabase/ssr`

## データベース
`supabase/migrations/001_init.sql` に定義済み。**スキーマは変更しないこと**（変更が必要な場合は理由を示して確認を取る）。

- 記録の単位は「1ゲーム = 1 session」。同じ日に複数ゲームを遊んだ場合は、同じ `played_on` の session が複数行になる
- `sort_order` で同じ日の中の並び順を管理する
- 参加者はゲーム（session）ごとに持つ
- ゲームの特徴はタグ（多対多）で表す。例：Among Us → 人狼系, 大人数 / Apex → FPS / 雑談 → 雑談
- 全テーブルに RLS 設定済み。`user_id` は `default auth.uid()` なので insert 時に指定不要
- 一覧・カレンダー表示には `session_detail` ビューを使う（tags: text[], participants: jsonb）

## 画面

### 1. ログイン（/login）
- 「Discordでログイン」ボタンのみ
- 未ログインで他ページにアクセスしたらここへリダイレクト（middleware）

### 2. カレンダー（/ ）
- 月表示。各日に、その日遊んだゲーム名を `sort_order` 順に表示
- 日付クリックで、その日の記録一覧（ゲーム名・タグ・参加者・メモ）をパネルかモーダルで表示
- 絞り込み：タグ（複数選択可）・ゲーム・参加者。絞り込み中は該当する記録だけをカレンダーに表示
- 右上に「記録する」ボタン

### 3. 記録の登録・編集（/sessions/new, /sessions/[date]/edit）
- 日付を選択し、その日に遊んだゲームを複数行で追加する
- 1行 = ゲーム（既存から検索して選択 or その場で新規作成）＋参加者（複数選択、その場で新規作成も可）＋メモ
- 「前の行の参加者をコピー」ボタン
- 行の並び替え（上下ボタンで可。ドラッグ&ドロップは任意）
- 保存時に session / session_person をまとめて登録する

### 4. ゲーム・タグ管理（/games）
- ゲーム一覧、タイトル編集、タグの付け外し
- タグ一覧、名前変更、削除
- 記録があるゲームは削除不可（DBで restrict 済み。エラー時はわかりやすいメッセージを出す）

### 5. 友人管理（/people, /people/[id]）
- 一覧：名前、一緒に遊んだ回数、最後に遊んだ日
- 詳細：名前・Discord ID・メモの編集、一緒に遊んだ記録の一覧（新しい順）

## MVPの対象外（今回は実装しない）
- 友人のDiscordアイコン取得（Bot連携は第2段階。現状は名前の頭文字アイコンで表示）
- Discordボイスチャンネルからの参加者自動検出
- マダミスの通過済み管理
- 他ユーザーとの記録共有

## 実装の進め方
以下の順で進め、**各フェーズの終わりで動作確認手順を示して一度止まること**。
1. プロジェクト初期化、Supabase接続、Discordログイン、middleware
2. ゲーム・タグ管理、友人管理（CRUD）
3. 記録の登録・編集フォーム
4. カレンダー表示と絞り込み
5. 友人詳細ページ、全体の見た目の調整

## その他
- 環境変数は `.env.local`（`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`）。`.env.example` も作成する
- UIの文言は日本語
- スマホでも使えるレスポンシブ対応